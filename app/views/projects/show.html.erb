<p id="notice"><%= notice %></p>

<p>
  <strong>Project:</strong>
  <%= @project.name %><br>
  <strong>Account:</strong>
  <%= @project.account.subdomain %><br>
<strong>Posts:</strong>
<%= link_to "Posts", project_posts_path(@project) %></p>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
